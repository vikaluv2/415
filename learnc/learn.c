#include <stdio.h>
#include <stdlib.h>

int main(){
    
    /*for(int i = 0; i < argc; i++){
        printf("Arg %d: %s\n", i, argv[i]);
    }*/
    
    printf("Enter a command =");
    char buffer[1000]; //declare stack array
    gets(buffer);
    //printf("Buffer[0] = %c\n", buffer[0]);
    //printf("*buffer = %c\n", *buffer);
    
}